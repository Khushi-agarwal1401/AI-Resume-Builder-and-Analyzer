# ── pnpm config for Vercel ────────────────────────────────────
# Vercel's serverless packaging can't handle pnpm's default symlinked
# node_modules. Hoisted linker creates a flat node_modules (like npm),
# which avoids "invalid deployment package from symlinked directories".
node-linker=hoisted